import { TrackListDirective } from './track-list.directive';

describe('TrackListDirective', () => {
  it('should create an instance', () => {
    const directive = new TrackListDirective();
    expect(directive).toBeTruthy();
  });
});
